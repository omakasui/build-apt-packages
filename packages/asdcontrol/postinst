#!/bin/sh
# postinst for asdcontrol

set -e

case "$1" in
    configure)
        # Reload udev rules to activate the HID device permissions.
        udevadm control --reload-rules 2>/dev/null || true
        udevadm trigger --subsystem-match=usb 2>/dev/null || true
        ;;
esac

exit 0
